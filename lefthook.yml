pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts,json,yml,yaml,md}'
      run: pnpm exec prettier --check {staged_files}

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

pre-push:
  commands:
    build:
      run: pnpm build
    test:
      run: pnpm test
